---
name: "youtube-all-in-one"
description: "一站式 YouTube/B站 视频处理：自动检测中文音轨 → 英文视频自动AI中文配音（edge-tts）→ 获取字幕 → 生成完整结构化学习文档（含思维导图、概念速查表、行动指南）。视频和笔记统一输出到同一文件夹。用户提供视频链接并要求下载/翻译/笔记/整理/学习文档时触发。"
---

# YouTube 一站式工作流（下载 + 学习笔记）

## 概述

将 YouTube/B站 视频**一站式处理**，从原始链接到最终产出只需一条指令：

```
视频链接 → [Phase 0] 获取中文标题
         → [Phase 1] 模式判断
              ├─ 学习模式（无"下载"） → 跳过下载，直接获取字幕
              └─ 学习下载模式（有"下载"） → 下载视频
                   ├─ 检测中文配音音轨 → 有 → 合并中文音轨 ✅
                   └─ 无 → 检测是否英文视频 → 是 → AI中文配音 → ✅
                                              └─ 否 → 保留原音轨
         → [Phase 2] 获取完整字幕
         → [Phase 2.5~2.8] AI 加工合成：翻译+章节总结 → 卡片渲染 → 双语字幕 → 合成最终视频
         → [Phase 3~6] 撰写结构化学习文档（含过程还原、可视化）
         → [Phase 7] 统一交付：视频(可选) + HTML 学习文档 → 同一文件夹
```

**本仓库整合了视频下载与学习笔记两大模块于一体：**

---

## 何时触发

**两种模式**：

| 模式 | 触发关键词 | 行为 |
|------|-----------|------|
| **学习** | "学习这个视频"、"帮我学习"、"分析这个视频"、"学习 [链接]" | 只获取字幕 + 生成结构化学习笔记（不下载视频） |
| **学习下载** | "学习下载"、"下载并学习"、"下载并整理"、"下载这个视频" | 下载视频 + 配中文音轨/字幕 + 生成笔记 |

**自动触发条件**（符合任一即触发）：
- 用户提供 YouTube/B站 链接 + 以下任一关键词：学习、下载、翻译、笔记、整理、总结、学习笔记、学习文档、听课、做笔记、配音、字幕
- 用户说："帮我学习这个视频" + 链接
- 用户说："帮我下载这个视频"、"下载并做笔记"、"把视频翻译成中文"、"生成学习文档"
- 用户说："帮我整理这个视频" + 链接

**不触发的情况**：
- 仅提供链接，没有明确操作意图
- 仅要求提取字幕（无笔记需求）→ 仅执行 Phase 0~2

> **关键区分**：用户是否明确说了"下载"。
> - 说"学习" → 不下载视频，只获取字幕和生成笔记
> - 说"学习下载" → 下载视频 + 字幕 + 笔记

---

## 前置条件

### 必需工具

| 工具 | 安装方式 | 用途 |
|------|---------|------|
| **Python 3.8+** | [python.org](https://python.org) | 所有脚本运行环境 |
| **yt-dlp** | `pip install yt-dlp` | YouTube/B站 下载核心 |
| **ffmpeg** | `winget install ffmpeg` | 音视频合并/处理 |
| **youtube-transcript-api** | `pip install youtube-transcript-api` | YouTube 字幕获取（首选） |
| **requests** | `pip install requests` | 视频元数据获取 |
| **Node.js** | `winget install nodejs` | yt-dlp JS runtime |

### 可选工具

| 工具 | 安装方式 | 用途 |
|------|---------|------|
| **faster-whisper** | `pip install faster-whisper` | 语音识别（AI配音必需） |
| **edge-tts** | `pip install edge-tts` | ⭐中文语音合成（AI配音核心，推荐优先安装） |
| **pyvideotrans** | [GitHub](https://github.com/jianchang512/pyvideotrans) | 全自动视频翻译配音（更强大但安装复杂） |
| **markdown** | `pip install markdown` | Markdown→HTML 降级输出 |

> **AI 配音建议**：建议至少安装 `edge-tts`（pip install edge-tts），它是 AI 配音最轻量可靠的方案。
> 安装后，所有英文视频下载后会自动配上中文语音。

### 批量安装

```bash
pip install -r requirements.txt
pip install yt-dlp edge-tts
```

### 网络要求

- 需要能够访问 YouTube（建议配置代理）
- 默认代理地址 `http://127.0.0.1:2080`（通过环境变量 `HTTP_PROXY` 或 `config.yaml` 修改）

---

## 项目结构

所有工具和配置统一放在同一个文件夹中：

```
YouTube-Download-Learning-Note/            ← 根目录（桌面）
├── .trae/skills/youtube-all-in-one/
│   └── SKILL.md                           ← 本 Skill 定义（AI 工作流指令）
├── scripts/
│   ├── yt-dl-zh.ps1                       ← ✦ 主下载脚本（Windows）
│   ├── yt-dl-zh.sh                        ← ✦ 主下载脚本（macOS/Linux）
│   ├── fetch_transcript.py                ←   字幕获取（YouTube/B站）
│   ├── get_yt_title.py                    ←   获取中文标题
│   ├── ai_dub_core.py                     ← ✦ AI配音核心（跨平台）
│   ├── ai-dub.ps1                         ←   AI配音包装器（Windows）
│   ├── ai-dub.sh                          ←   AI配音包装器（macOS/Linux）
│   ├── render_cards.py                    ←   章节总结卡片渲染（Phase 2.6）
│   ├── build_bilingual_ass.py             ←   双语 ASS 字幕生成（Phase 2.7）
│   ├── compose_video.py                   ←   最终合成（Phase 2.8）
│   ├── compose_chunked.py                 ←   分段合成（长视频）
│   ├── edge_dub.py                        ←   edge-tts 配音管线
│   ├── yt_monitor.py                      ← ✦ 频道监控核心（跨平台）
│   ├── yt-monitor.ps1                     ←   频道监控包装器（Windows）
│   ├── yt-monitor.sh                      ←   频道监控包装器（macOS/Linux）
│   └── youtuber_list.md                   ←   监控频道列表
├── config.yaml                            ←   配置文件
├── requirements.txt                       ←   Python 依赖
├── .gitignore
└── README.md                              ←   项目说明
```

**脚本路径常量（供后续引用）**：

```
$SCRIPT_DIR   = "C:\Users\周春宇\Desktop\YouTube-Download-Learning-Note\scripts"
$CONFIG_PATH  = "C:\Users\周春宇\Desktop\YouTube-Download-Learning-Note\config.yaml"
$ROOT_DIR     = "C:\Users\周春宇\Desktop\YouTube-Download-Learning-Note"
```

---

## Phase 0：获取中文标题

下载任何内容之前，先使用中文 UA 请求 YouTube 页面，获取视频的中文标题用于文件名和后续笔记。

### 方法

```powershell
# 使用 get_yt_title.py
python "$SCRIPT_DIR\get_yt_title.py" "<URL>"

# 输出：URL \t 中文标题
# 回退方案：yt-dlp --print "%(title)s"
```

### 执行后反馈给用户

- 视频中文标题
- 是否有中文标题（含中文标记 ✅）
- 文件命名预览

---

## Phase 1：视频处理（学习模式 vs 学习下载模式）

### 模式判断

**学习模式**（用户说"学习"但没说要"下载"）：
- 跳过视频下载，直接进入 Phase 2 获取字幕
- 获取字幕后直接进入 Phase 3~6 生成笔记
- 最终产出：HTML 学习笔记 + 字幕文件（无视频文件）

**学习下载模式**（用户说"学习下载"或明确要求"下载"）：
- 执行完整视频下载流程
- 获取字幕
- 生成笔记
- 最终产出：视频文件 + HTML 学习笔记 + 字幕文件

> **关键区分**：用户是否明确说了"下载"。
> - "学习这个视频" → 学习模式（不下载）
> - "学习下载这个视频" → 学习下载模式（下载）

---

### 学习下载模式：下载与中文处理

**优先级链**：🥇中文配音音轨(140-16) → 🥈AI中文配音（英文视频，edge-tts） → 🥉保留原始音轨

1. **下载视频**（最佳画质优先）
2. **检测中文配音音轨**（yt-dlp -F 检测 140-16 格式）
3. **如果已有中文配音音轨** → 直接合并，完成 ✅
4. **如果没有中文配音音轨**：
   - 判断是否为英文视频
   - 如果是英文视频 → **运行 AI 中文配音**（edge-tts / ai_dub_core.py）
   - 如果不是英文视频 → 保留原始音轨

> **AI 配音前提**：需要安装 `edge-tts`（`pip install edge-tts`）和 `faster-whisper`（`pip install faster-whisper`）。
> 如果未安装，则保留原始音轨并提示用户。

### 1a. 下载视频（基础命令）

```powershell
# 下载到输出目录（不自动AI配音，仅下载）
# 最终输出目录由 Phase 9 统一管理

# 先用 yt-dlp 下载最佳画质视频（临时输出到 TEMP）
$title = "..."  # 从 Phase 0 获取
$tempDir = "$env:TEMP\yt_download_${title}"
mkdir $tempDir -Force > $null

# 检测中文音轨
python -m yt_dlp --no-check-certificates --js-runtimes node -F "<URL>" 2>&1 | Select-String "140-16"

# 有中文音轨 → 下载含中文音轨版本
python -m yt_dlp --no-check-certificates --js-runtimes node `
  -f "bv[ext=mp4]+140-16/bv*+ba/best" --merge-output-format mp4 `
  -o "$tempDir\${title}.%(ext)s" "<URL>"

# 无中文音轨 + 英文视频 → 下载后 AI 配音
python -m yt_dlp --no-check-certificates --js-runtimes node `
  -f "bv*+ba/best" --merge-output-format mp4 `
  -o "$tempDir\${title}.%(ext)s" "<URL>"

# 如果下载失败（反爬），尝试 Android 客户端模式
python -m yt_dlp --extractor-args "youtube:player_client=android" `
  --no-check-certificates -f "bv*+ba/best" --merge-output-format mp4 `
  -o "$tempDir\${title}.%(ext)s" "<URL>"
```

### 1b. AI 中文配音（学习下载模式）

当检测到视频为**英文内容且无中文音轨**时，自动执行：

```powershell
# 使用 ai_dub_core.py（跨平台完整实现）
python "$SCRIPT_DIR\ai_dub_core.py" "$videoPath" --voice "zh-CN-XiaoxiaoNeural"

# 或保留原背景音混合
python "$SCRIPT_DIR\ai_dub_core.py" "$videoPath" --keep-bgm

# 如果已有翻译好的中文文本，直接指定
python "$SCRIPT_DIR\ai_dub_core.py" "$videoPath" --text "$videoPath\..\translated.txt"

# 配音后的文件命名为：{标题}_zh_dubbed.mp4
```

**ai_dub_core.py 完整流程**：
1. 提取音频（ffmpeg）
2. faster-whisper 转录英文文本
3. 保存转录文件供用户翻译
4. 用户翻译后重新运行，指定 `--text` 参数
5. Edge TTS 分段合成中文语音
6. ffmpeg 合并音频与视频

### 1c. 频道监控

```powershell
# Windows
powershell -File "$SCRIPT_DIR\yt-monitor.ps1"
powershell -File "$SCRIPT_DIR\yt-monitor.ps1" -DryRun
powershell -File "$SCRIPT_DIR\yt-monitor.ps1" -Channel Name

# macOS / Linux
bash "$SCRIPT_DIR/yt-monitor.sh"
bash "$SCRIPT_DIR/yt-monitor.sh" --dry-run
bash "$SCRIPT_DIR/yt-monitor.sh" --channel Name
```

**yt_monitor.py 功能**：
- 扫描配置的 YouTube 频道最新视频
- 解析 ytInitialData 提取视频列表
- SQLite 数据库存储已下载视频，避免重复
- 过滤含中文标题的视频
- 自动使用 yt-dlp 下载新视频

### 流程图（新）

```
用户输入 URL
  │
  ├─ Step 1: get_yt_title.py → 获取中文标题，判断语言
  │
  ├─ 模式判断
  │   ├─ 用户说"学习"（无"下载"） → 学习模式
  │   │   └─ 跳过下载，直接进入 Phase 2 获取字幕
  │   │
  │   └─ 用户说"学习下载"或"下载" → 学习下载模式
  │       ├─ Step 2: 下载视频（最佳画质）
  │       │
  │       ├─ Step 3: 检测中文配音音轨 (140-16)
  │       │   ├─ 有 ✅ → 视频已有中文配音 → 直接使用
  │       │   │
  │       │   └─ 无 → 判断视频语言
  │       │       ├─ 是英文视频 → AI中文配音 (ai_dub_core.py)
  │       │       │   ├─ edge-tts + faster-whisper 可用 → 转录+TTS+合并 → ✅
  │       │       │   └─ 不可用 → 保留原音轨 + 提示安装
  │       │       │
  │       │       └─ 非英文视频 → 保留原始音轨 ✅
  │       │
  │       └─ 视频文件准备完毕
  │
  ├─ Step 4: 获取字幕（Phase 2）
  │
  ├─ Step 5~8: 生成结构化学习文档（Phase 3~6）
  │
  └─ Step 9: 统一交付（Phase 9）
       ├─ 学习模式：HTML 笔记 + 字幕文件（无视频）
       └─ 学习下载模式：视频 + HTML 笔记 + 字幕文件
```

---

## Phase 2：获取字幕（用于学习文档）

使用 `fetch_transcript.py` 获取完整字幕，支持**多级降级**确保任何环境都能产出内容。

### 命令

```bash
# YouTube（输出到临时目录）
python "$SCRIPT_DIR\fetch_transcript.py" "<URL>" --output "$env:TEMP\yt_notes"

# Bilibili
python "$SCRIPT_DIR\fetch_transcript.py" "https://www.bilibili.com/video/BVxxxxxxxxxx"

# 指定语言
python "$SCRIPT_DIR\fetch_transcript.py" "<URL>" --lang zh-Hans

# yt-dlp 优先（当 youtube-transcript-api 失败时）
python "$SCRIPT_DIR\fetch_transcript.py" "<URL>" --ytdlp-first
```

### 多级降级策略

| 级别 | 方法 | 说明 |
|------|------|------|
| **Level 1** | `fetch_transcript.py` | 首选：youtube-transcript-api → 降级 yt-dlp |
| **Level 2** | 第三方摘要平台 | VideoHighlight / Glasp / Eightify（标注"基于第三方"） |
| **Level 3** | 网络文字回顾 | 搜索引擎找博主回顾、论坛讨论 |
| **Level 4** | 框架性笔记 | 基于标题+简介+评论，标注"无法获取完整内容" |

> **绝不因为拿不到字幕就告诉用户"做不了"。**

### 字幕输出文件

脚本自动生成以下文件到临时目录：
- `{video_id}_transcript.json` — 带时间戳的完整字幕（AI 分析用）
- `{video_id}_transcript.txt` — 纯文本（AI 阅读用）
- `{video_id}_timestamped.txt` — 带 `[分:秒]` 时间戳的文本
- `{video_id}_info.json` — 视频元数据

### 执行后反馈

获取成功后向用户报告：
- 视频标题、总时长、平台
- 视频类型判断（授课/访谈/教程/故事）
- 预估主题数
- 快速扫描到的人名/数据/案例清单

---

## Phase 2.5：翻译与章节总结（AI 执行）

AI 读取 Phase 2 得到的 `{video_id}_transcript.json`，产出两份文件：

1. **bilingual transcript JSON**：在原 transcript 的每个 segment 上补 `text_zh` 字段（中文翻译），其余字段（text/start/duration）保持不动。
2. **`chapters.json`**（契约固定，供 Phase 2.6/2.8 使用）：

```json
[{"index":1,"start":0.0,"end":200.0,"title":"科学世界观的奠基","points":["要点一","要点二","要点三"]}]
```

- 按内容逻辑分章（通常 3~8 章），`start`/`end` 单位为秒，各章首尾衔接覆盖全片
- `title` 为章节中文标题（建议 ≤ 14 字），`points` 为 2~4 条核心要点（每条建议 ≤ 30 字）

## Phase 2.6：渲染总结卡片

```bash
python "$SCRIPT_DIR/render_cards.py" chapters.json --out-dir cards/
```

- 产出 `cards/card_001.png …`（1920x1080 透明背景：藏青半透明圆角卡片 + 金色标题与下划线 + 白色要点）
- 中文字体自动回退链：msyh.ttc → simhei.ttf → simsun.ttc → Pillow 默认

## Phase 2.7：双语字幕

```bash
python "$SCRIPT_DIR/build_bilingual_ass.py" bilingual_transcript.json --out bilingual.ass
```

- 顶部英文小字（EnTop，Alignment=8）+ 底部中文大字（ZhBottom，白色加粗黑描边，Alignment=2）
- 无 `text_zh` 的段落默认不出中文字幕行（`--zh-fallback en` 可改为复用英文）

## Phase 2.8：合成最终视频

```bash
python "$SCRIPT_DIR/compose_video.py" --video "{原视频}.mp4" --audio "{Phase 1 配音音频}.mp3" \
  --chapters chapters.json --cards-dir cards/ --ass bilingual.ass --out "{标题}.mp4"
```

- 画面：原视频缩放铺满后压暗+模糊当背景，按 chapters.json 时间段叠加总结卡片，最后烧录双语字幕
- 音频：配音音轨替换原音（配音来自 Phase 1 的 ai-dub 产出，`-shortest` 截齐）
- ffmpeg 自动发现：PATH → imageio-ffmpeg 自带二进制；启动时自动探测 subtitles filter（libass），不支持则给出降级方案
- 可选参数：`--crf 23`（画质）、`--size 1920x1080`（输出尺寸）

---

## Phase 3：判断视频类型

拿到字幕内容后，先判断视频类型，不同采用不同的结构侧重：

| 类型 | 识别特征 | 结构侧重 | 典型例子 |
|------|---------|---------|---------|
| **授课/讲座类** | 课程、讲座、分享、演讲 | 按知识点递进，强调逻辑链条和论证路径 | 大学课程、TED、技术分享 |
| **访谈/对话类** | 对话、访谈、播客、对谈 | 按话题切分，保留双方互动，突出核心嘉宾观点 | The Diary of a CEO、播客节目 |
| **教程/操作类** | 教程、教学、上手、从零开始 | 按步骤切分，强调可复现性，附代码/命令说明 | 编程教程、软件操作、手工教程 |
| **故事/案例类** | 故事、传记、案例、复盘 | 按情节推进，保留叙事节奏，结尾提炼启示 | 人物传记、商业案例分析 |

> 类型不明确时，默认按「访谈/对话类」处理。

---

## Phase 4：强制预扫描

通读获取的全部字幕内容，建立「内容清单」（内部流程，不输出给用户）：

- 总主题数、总子论点数
- 人名清单（全部，含身份背景）
- 书名/文章名/论文名（全部）
- 案例/故事清单（全部，标注时间范围）
- 关键数据/统计（全部）
- 关键引语（每个主题至少 3 句备选）
- 反问/讽刺/夸张/戏剧化段落（全部）
- 提到的工具/方法/实践建议（全部）

> 写完笔记后必须拿清单逐条核对，漏一项就返工。

---

## Phase 5：主题切分

按**内容逻辑**切分主题，每个主题对应一个完整的"教学单元"：

- 每个主题包含 **3-6 个论点段落**，不得少于 3 个
- 如果某个主题信息量不足 3 个论点段落，合并到相邻主题
- 不得为了凑数把一个主题拆成多个薄弱主题
- **访谈类**：按话题自然转换切分，不要按时间均分
- **教程类**：按操作步骤/模块切分，确保每个主题可独立复现

---

## Phase 6：撰写学习文档

### 核心原则

> **不是做摘要，而是做"学习替身"——覆盖全部重要信息，但用自然叙述呈现。**
>
> **写完自己读一遍：如果读起来像"机器提取的数据报告"或"百度百科词条"，就重写。**
>
> **内容少了的笔记=废笔记。宁可多写不可少写。**
>
> **过程还原不到位=转述。如果笔记只给结论不给过程，就是高级摘要。**
>
> **推导步骤丢失=废笔记。读者知道"结论是什么"，但不知道"为什么"。**

### 文档整体结构

```
┌─────────────────────────────────────┐
│  封面区（标题、嘉宾、时长、简介）     │
├─────────────────────────────────────┤
│  为什么值得看（100字价值定位）       │
├─────────────────────────────────────┤
│  目录导航                           │
├─────────────────────────────────────┤
│  整课骨架（论证路径概括）            │
├─────────────────────────────────────┤
│  全局概念导图（Mermaid）            │
├─────────────────────────────────────┤
│  主题一 ~ 主题N（核心内容）          │
│  每个主题包含（四件套）：             │
│  · 时间段标注（仅主题开头一次）       │
│  · 本块学习地图（1段：定位与作用）    │
│  · 详细内容（连贯叙述段落）          │
│    · 每段三层：背景→展开→过渡        │
│    · 用"首先…随后…然后…最后…"连接   │
│  · 关键引用（原文+中译+修辞意图）     │
│  · 核心概念表 + 结构梳理（表/Mermaid）│
├─────────────────────────────────────┤
│  核心概念速查表                      │
├─────────────────────────────────────┤
│  延伸阅读与资源                      │
├─────────────────────────────────────┤
│  附录：人物索引/概念索引/资料引用     │
├─────────────────────────────────────┤
│  最终核查（覆盖数 + 遗漏检查）        │
└─────────────────────────────────────┘
```

### 每个主题的叙述要求（连贯叙述体）

每个主题的正文必须写成**连贯叙述段落**，像"有人给你完整讲了一遍这段内容"，而不是机器提取的要点：

1. **三层段落结构**：每个论点段落必须有——背景/语境（1-3 句：讲者为什么谈到这个）→ 核心展开（5-10 句：论点、证据、案例细节，必须有具体人名、数字、事件，禁止"有人""某地""曾经"式模糊替代）→ 过渡/回应（1-2 句：与下一点的逻辑关联）。
2. **保留推导过程**：讲师用了 N 步论证到达结论，叙述里就要保留 N 步；质疑→回应、犹豫→补充→修正的完整来回不得压缩。
3. **用连接词建立逻辑流**："首先""随后""这里值得注意的是""然后""最后"，让段落之间像河流一样推进，而不是并列的积木。
4. **低价值内容融入上下文**：寒暄、纯过渡、技术操作（"我们看下一页"）并入叙述，不单独成段。

### 叙述的深度分层

| 内容类型 | 策略 | 理由 |
|---------|------|------|
| **核心观点/关键转折** | 完整还原（保留犹豫→补充→修正的完整过程） | 展示思维复杂性 |
| **次要观点/辅助说明** | 保留关键转折点，压缩琐碎细节 | 保留转折，删除停顿 |
| **事实性回答** | 直接陈述结论 | 没有思想演进过程 |
| **寒暄/过渡** | 一句话带过 | 不值得展开 |

### 叙述规则

**禁止机械罗列**：
```
❌ 坏：- 讲师解释... - 讲师提到... - 讲师指出...
（bullet 列表 + 连续"讲师+动词"，像机器提取的数据）
```

**禁止摘要腔**：
```
❌ 坏写法："这本书主要讲了……然后又讲了……最后讲了……"
❌ 坏开头："本质上""归根结底""简而言之""总体而言"
```

**必须写成连贯叙述**：
```
✅ 好写法：
开头讲师先回应了方法论上的批评——他上周发了课之后，导师给他写了封邮件，
说他讲课太斩钉截铁。讲师自己也承认：他的风格确实是"凭直觉把复杂东西讲得
简单清楚"。然后他解释了这个学期为什么要往这个方向走……
（信息完整 + 自然流动，像朋友转述给你听）
```

**必须遵守的规则**：
- 正文用连贯段落，**禁止使用 bullet 列表**（概念表、索引表除外）
- 引语穿插在叙述中或集中在"关键引用"小节，用 `>` 引号标出
- 用"首先""随后""讲师转而指出""最后他总结道"等连接词还原时间线
- 连续 3 句以上以"讲师+动词"开头 = 立即重写
- **禁止口语化逐句还原**：不要写"他说…然后他说…接着他又说…"
- 提取逻辑骨架，用因果关系连接，不是按时间顺序复述每一句话
- 每个主题配"关键引用"小节：原文 + 中文翻译 + 修辞意图（讽刺/强调/反问/夸张）
- 每个主题配"核心概念表"：术语中英对照；涉及对比/分类必配表格，涉及多概念关联必配 Mermaid 图

### 引语处理（重要）

所有英文引语必须加中文翻译：

```html
<blockquote>
  <p>"English quote text here..."<br><span class="trans">"中文翻译文本..."</span></p>
  <cite>—— Peter Jiang</cite>
</blockquote>
```

### 字数要求

| 视频时长 | 笔记最低总字数 | 信息密度高时（访谈/时政/数据密集） |
|---------|---------------|----------------------------------|
| < 15 分钟 | ≥ 2,500 字 | 可到 4,000 字 |
| 15-30 分钟 | ≥ 4,500 字 | 可到 7,000 字 |
| 30-60 分钟 | ≥ 8,000 字 | 可到 12,000 字 |
| 60-120 分钟 | ≥ 12,000 字 | 可到 20,000 字 |
| > 120 分钟 | ≥ 16,000 字 | 可到 26,000 字 |

> **下限是硬指标，上限由信息完整性决定，不设固定比例封顶。**
>
> - 停止条件不是"字数够了"，而是"Phase 4 预扫描清单的全部条目已覆盖"
>   （人名、书名、案例、数据、引语、反问/讽刺段落零遗漏）。
> - 信息密度高的内容（多方观点交锋、数据环环相扣、论证链条长）允许写到
>   右列上限甚至略超——内容少了的笔记=废笔记。
> - 但禁止注水凑字：同一论点不重复展开、不写套话、不逐句复述原文。
>   多余的字数必须换来新的信息增量。
> - 信息密度低的内容（故事类、闲聊类）达到下限即可收尾。

---

## Phase 7：学习增强模块

### 1. 核心概念速查表（必选）

| 概念 | 一句话定义 | 实践意义 |
|------|----------|---------|
| xxx | ... | ... |

从全文中提炼 8-15 个最核心的概念。

### 2. 延伸阅读与资源（必选）

整理视频中提到的书籍、论文、人物、工具、相关视频。

### 3. 附录索引（必选）

- **人物索引**：人名 / 首次出现 / 身份角色 / 在内容中的关联
- **概念索引**：概念 / 英文 / 定义 / 出现主题
- **资料引用**：视频中提到的书籍、报道、文件、帖子等全部出处

### 4. 最终核查（必选）

- 预估语义段落数 vs 实际覆盖段落数
- 关键信息遗漏检查：人名__个、书名__本、案例__个、数据__条——全部已覆盖

### 5. 自测思考题（可选，适合授课/教程类）

每章 2-3 道：理解类、应用类、拓展类。

---

## Phase 8：可视化规范

| 内容类型 | 推荐可视化 | 工具 |
|---------|-----------|------|
| 概念层级/分类 | 思维导图 | Mermaid mindmap |
| 流程/机制/步骤 | 流程图 | Mermaid flowchart |
| 多维度对比 | 对比表格 | HTML table |
| 数据/统计 | 图表 | ECharts |
| 时间序列 | 时间线 | HTML / Mermaid gantt |
| 核心概念 | 信息卡片 | HTML cards |
| 因果/关系网络 | 关系图 | Mermaid graph |

**基本要求**：
- 每个主题至少 **1 个可视化元素**
- 全文至少 **1 个全局概念导图**
- 数据类内容必须配图表
- 避免同一种可视化形式重复使用超过 3 次

---

## Phase 9：文件输出与交付

### 统一交付结构（视频 + 学习文档 → 同一文件夹）

所有产出统一放在一个文件夹中，视频和笔记在一起，一目了然：

```
桌面/学习笔记产出/                          ← 根目录
└── {视频标题}_学习笔记/                    ← 每个视频一个独立文件夹（包含所有产出）
    ├── {视频标题}_学习笔记.html            ← 主文档（直接浏览器打开）
    ├── {视频标题}.mp4                      ← 合成后视频（压暗背景 + 章节总结卡片 + 双语烧录字幕 + 配音音轨）
    │    或 {视频标题}_zh_dubbed.mp4        ← 未走合成流程时，为 AI配音版本（如适用）
    ├── bilingual.ass                       ← 双语字幕（顶部英文 + 底部中文，可外挂）
    ├── chapters.json                       ← 章节总结（总结卡片数据源）
    ├── {video_id}_transcript.json          ← 字幕 JSON
    ├── {video_id}_transcript.txt           ← 字幕纯文本
    ├── {video_id}_timestamped.txt          ← 带时间戳字幕
    └── {video_id}_info.json                ← 视频元数据
```

> **设计原则**：同一个视频的所有产出（视频、笔记、字幕）放在一个文件夹里。
> 用户打开一个文件夹，视频和笔记都在手边，看完视频直接看笔记，非常清晰。

### 配置参考

下载设置通过 `config.yaml` 控制，但注意视频最终会移动到统一输出文件夹中：

```yaml
# $ROOT_DIR\config.yaml

proxy: 'http://127.0.0.1:2080'
# download_dir 仅用于临时下载，最终视频会移到统一输出文件夹

format:
  best: 'bv[ext=mp4]+140-16/bv*+ba/best'     # 中文音轨优先，否则最佳画质+最佳音频
  subs: 'bv[ext=mp4]+ba[ext=m4a]'               # 字幕模式
  audio: '140-16/ba*'                            # 音频模式

ai_dub:
  engine: 'edge-tts'                      # 默认使用 edge-tts（轻量可靠）
  voice: 'zh-CN-XiaoxiaoNeural'           # Edge TTS 语音名称
  source_language: 'en'                    # 源语言（英文）
  target_language: 'zh-cn'                # 目标语言（中文）
```

### 工作目录隔离原则（强制）

- **YouTube 学习笔记工具文件夹** 最终不得出现任何新的产出文件
- 所有中间生成文件必须在交付前删除或移动至对应输出目录
- 工作目录应始终保持干净，只保留工具脚本和技能说明

### 自动清理

交付完成后，立即删除工作目录中所有中间产出：
- 主题切分文件、子代理生成的分块文件
- 所有临时脚本、临时目录（`.ytdlp_tmp_*`、`.bili_tmp_*`）
- 之前任务遗留的旧文件

---

## 十三条铁律（绝对禁止）

1. ❌ **遗漏**核心信息：人名、书名、案例、数据，缺一项都不行
2. ❌ **机械罗列**：连续使用"讲师+动词"句式超过 2 句
3. ❌ **总结式写作**：把 10 分钟内容压成 2 行
4. ❌ **压平语气**：把反问改陈述、把讽刺改中性
5. ❌ **空洞套话**："这个问题非常复杂"等废话
6. ❌ **正文用 bullet 列表**：正文必须是连贯段落（概念表、索引表除外）
7. ❌ **到处时间戳**：仅主题开头标注一次
8. ❌ **模糊表述**：写"等等""之类""一些"
9. ❌ **摘要式流水账**："主要讲了……然后又讲了……最后讲了……"
10. ❌ **一步到位压缩推导**：讲师用了N步，笔记必须保留N步
11. ❌ **只记最终回应**：质疑→回应的完整来回必须保留
12. ❌ **只记嘉宾最终答案**：犹豫→补充→修正必须保留
13. ❌ **口语化逐句还原**：禁止"他说…然后他说…"翻译腔

---

## 自审清单（交付前逐项核对）

### 内容完整性
- [ ] 总字数达标（按时长对照表）
- [ ] 每个主题至少 3 个论点段落
- [ ] 每个人名都保留了（首次出现带英文+身份）
- [ ] 每本书/文章名都保留了
- [ ] 每个案例都有完整细节
- [ ] 所有数据都准确保留
- [ ] 每个主题至少 1 处直接引语（原文+中译+修辞意图）
- [ ] 反问/讽刺/夸张都保留了语气

### 叙述质量 + 过程完整性
- [ ] 没有机械罗列（连续"讲师+动词"不超过 2 句）
- [ ] 正文全部是连贯段落，无 bullet 列表
- [ ] 每个论点段落都有背景→展开→过渡三层
- [ ] 讲师用了N步推导，笔记保留了N步
- [ ] 质疑→回应的完整来回被保留
- [ ] 访谈中的犹豫→补充→修正过程被保留
- [ ] 没有口语化逐句还原
- [ ] 没有"本质上/归根结底/简而言之"式摘要腔开头
- [ ] 字数由信息完整性驱动：预扫描清单条目全覆盖，新增字数均有信息增量，没有注水硬凑

### 结构与可视化
- [ ] 有全局概念导图
- [ ] 每个主题至少 1 个可视化元素（表格或 Mermaid）
- [ ] 可视化类型多样化
- [ ] 有核心概念速查表
- [ ] 有附录索引（人物/概念/资料引用）
- [ ] 有最终核查（覆盖数+遗漏检查）
- [ ] 有延伸阅读与资源

### 输出与交付
- [ ] HTML 文档生成成功，零外部依赖
- [ ] 视频文件下载完成（含中文配音或原始音频）
- [ ] 总结卡片已按章节时间段叠加到画面
- [ ] 双语字幕已烧录（顶部英文小字 + 底部中文大字）
- [ ] 配音音轨已替换原始音轨
- [ ] 字幕文件已保存
- [ ] 文件命名规范
- [ ] **所有产出（视频 + 文档 + 字幕）统一放入 `桌面/学习笔记产出/{视频标题}_学习笔记/` 同一文件夹中**
- [ ] 工作目录已清理干净（无临时文件残留）

---

## 环境变量

| 变量 | 用途 | 默认值 |
|------|------|--------|
| HTTP_PROXY | HTTP 代理地址 | http://127.0.0.1:2080 |
| HTTPS_PROXY | HTTPS 代理地址 | 同上 |
| YT_DL_DIR | 视频下载目录 | ~/youtube-downloads |

---

## 跨平台说明

| 项目 | Windows | macOS/Linux |
|------|---------|-------------|
| Python | `python` | `python3` |
| 路径分隔 | `\` | `/` |
| 桌面路径 | `C:\Users\{用户名}\Desktop\` | `~/Desktop/` |
| 下载脚本 | `yt-dl-zh.ps1` | `yt-dl-zh.sh` |
| AI配音脚本 | `ai-dub.ps1` (包装器) | `ai-dub.sh` (包装器) |
| AI配音核心 | `ai_dub_core.py` | `ai_dub_core.py` |
| 频道监控 | `yt-monitor.ps1` (包装器) | `yt-monitor.sh` (包装器) |
| 频道监控核心 | `yt_monitor.py` | `yt_monitor.py` |
| 字幕脚本 | `fetch_transcript.py` | `fetch_transcript.py` |
